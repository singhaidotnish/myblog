title: Hello, world! I'm Nishith Singhai
description: > # this means to ignore newlines until "baseurl:"
  If you are just browsing. Step inside.
permalink: ':title/'
baseurl: "/myblog" # the subpath of your site, e.g. /blog
url: "https://singhaidotnish.github.io" # the base hostname & protocol for your site, e.g. http://example.com
site-twitter: #if your site has a twitter account, enter it here

# Author Settings
author: Nishith Singhai # add your name
author-img: nishith_singhai_white_background.png # add your photo
about-author: I am a developer of sorts. I develop ways to rewire my ADHD. Develop tools for VFX pipeline. Always hungry to keep learning.
social-twitter: # add your Twitter handle
social-facebook: # add your Facebook handle
social-github: singhaidotnish # add your Github handle
social-linkedin: # add your Linkedin handle
social-email: singhaidotnish[at]gmail[dot]com

# Tracker
analytics: # Google Analytics

# Build Settings
markdown: kramdown
plugins:
  - jekyll-sitemap
  - jekyll-paginate
  - jemoji

paginate: 8
paginate_path: "/page/:num"

exclude: ["node_modules", "gulpfile.js", "package.json", "yarn.lock"]

# greer jekyll plugin

gallery:
  dir: photos               # Path to the gallery
  symlink: false            # false: copy images into _site. true: create symbolic links (saves disk space)
  title: "Photos"           # Title for gallery index page
  title_prefix: "Photos: "  # Title prefix for gallery pages. Gallery title = title_prefix + gallery_name
  sort_field: "date_time"   # How to sort galleries on the index page.
                            # Possible values are: title, date_time, best_image
  thumbnail_size:
    x: 400                  # max width of thumbnails (in pixels)
    y: 400                  # max height of thumbnails (in pixels)
  # The following options are for individual galleries.
  galleries:
    chile_trip:
      best_image: IMG_1068.JPG  # The image to show on the gallery index page. Defaults to the last image.
    japan_trip:
      best_image: IMG_0690.JPG
      name: "日本の旅"       # Defaults to directory name, replacing _ with spaces & capitalizing words.
    awesome_stuff:
      best_image: snaileo_gonzales.jpg
      sort_reverse: true    # Reverse sort images in gallery.
    secret_stuff:
      hidden: true          # Don't show this gallery on the index page. People must guess the URL.
    with_info:
      info:
        desc: "Gallery Description" # Info fields can be used in custom templates.


# simple image gallery

static_files = [